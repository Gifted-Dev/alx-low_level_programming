#include "main.h"
/**
 * _islower - a function used to check if an
 * alphabet is lowercase..
 * @c: character to be checked
 * Return: return 1 if c is lowercase
 */
int _islower(int c)
{
	return (c > 'a' && c <= 'z');
}
